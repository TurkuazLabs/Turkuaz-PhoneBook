<!--
# 📄 Dosya Yolu: C:/Projects/TelefonRehberi/mobile/android/app/src/main/AndroidManifest.xml
# 📌 Amac: Android rehber izinlerini, ag erisimini, yerellestirilmis uygulama adini ve backup politikasini tanimlar.
# 📌 Config - XML
# Version: 1.0.2
# Aciklama: READ_CONTACTS, WRITE_CONTACTS, LAN HTTP senkron izinleri, @string/app_name etiketi ve cihaz/sync kimliklerinin baska cihaza restore edilmesini onleyen backup politikasini tanimlar.
# Bagimli Oldugu Katman: Config | View | Language
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
