<!--
# 📄 Dosya Yolu: C:/Projects/TelefonRehberi/mobile/android/app/src/main/AndroidManifest.xml
# 📌 Amac: Android rehber izinlerini, ag erisimini ve yerellestirilmis MainActivity uygulama adini tanimlar.
# 📌 Config - XML
# Version: 1.0.1
# Aciklama: READ_CONTACTS, WRITE_CONTACTS, LAN HTTP senkron izinleri ve @string/app_name tabanli yerellestirilmis etiketi tanimlar.
# Bagimli Oldugu Katman: Config | View | Language
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
